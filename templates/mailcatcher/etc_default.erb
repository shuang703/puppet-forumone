APP_NAME="Mailcatcher"
APP_FILENAME="mailcatcher"
APP_PID="/var/run/$APP_FILENAME.pid"
APP_PATH="<%= path %>"
APP_FILE="mailcatcher"
APP_LOGS="/var/log/mailcatcher"
GEMFILE_LOCATION="/opt/puppet/mailcatcher"
APP_PRE_OPTION="bundle exec"
APP_POST_OPTION="<%= @options.join(' ') %> -f"
